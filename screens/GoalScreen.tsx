import React from "react";
import { Text, View } from "react-native";

export default function GoalScreen() {
  return (
    <View>
      <Text>
        This screen will show an individual goal, the user's goal stats, a
        button to 'track' a goal (mark as successful), and a pop-up calendar to
        track missed days.
      </Text>
    </View>
  );
}
